React and AWS Lambda functions on client side and Node.js (Express, Passport and JWT) on server.

Client can be used independently (except for facebook authentication, which currently needs a server running).